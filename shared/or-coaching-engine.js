/*
 * DEPRECATED & NEUTRALIZED (2026-06-16) — IP protection.
 * The Open Response coaching/evaluation logic has been moved server-side and is
 * served exclusively by the authenticated `evaluate-or` Supabase Edge Function.
 * No pedagogical reasoning, rubric, or prompt logic is shipped to the browser.
 * This stub remains only to avoid 404s from any stale reference.
 */
;(function(){ /* intentionally empty */ })();
